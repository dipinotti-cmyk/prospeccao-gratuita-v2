# Obrigatórias — sem isso o painel não sobe (leads/dashboard)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Opcional — ativa a busca automática de leads via Google Maps
APIFY_TOKEN=
APIFY_ACTOR_ID=

# Opcional — ativa a geração automática de mensagem por IA
OPENAI_API_KEY=

# Opcional — protege a rota /api/cron-followups
CRON_SECRET=
